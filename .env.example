# Copy to .env and fill in with your runtime configuration.
GRAPH_ID=your-graph-id
BACKEND_BASE_URL=http://127.0.0.1:8000
# Optional; leave blank if the backend does not require an API key.
BACKEND_API_KEY=
# Optional overrides
TIMEOUT_SECONDS=10
LOG_LEVEL=INFO
